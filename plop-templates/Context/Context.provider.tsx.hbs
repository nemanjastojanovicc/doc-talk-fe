import React from 'react'
import {{pascalCase name}}Context from './{{pascalCase name}}.context';

type {{pascalCase name}}Props = {};

const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = (props) => {
  const { children } = props;

  return <{{pascalCase name}}Context.Provider value=\{{}}>{children}</{{pascalCase name}}Context.Provider>
};

export default {{pascalCase name}};